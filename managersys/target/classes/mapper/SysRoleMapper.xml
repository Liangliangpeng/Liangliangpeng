<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.operatormanager.user.mapper.SysRoleMapper">


	<select id="findRoleNameByUserId" resultType="String">
		SELECT sr.role_desc FROM sys_user_role sur LEFT JOIN sys_role sr ON sr.id = sur.role_id
            LEFT JOIN sys_user su ON sur.user_id = su.id WHERE su.id = #{userId}
	</select>

</mapper>
